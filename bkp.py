# select year in datepicker
   if current_date.year > datepicker_date[2]:
        # next arrow for year
        pass

    while current_date.year == datepicker_date[2]:

        # select month
        if current_date.month > datepicker_date[1]:
            # next arrow for month
            pass

        # while current_date.month == datepicker.month
        while current_date.month == datepicker_date[1]:
            # select day
